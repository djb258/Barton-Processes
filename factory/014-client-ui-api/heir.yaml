# Process HEIR — 014-client-ui-api
process_id: "PROC-CLIENT-UI-API"
process_number: "014"
name: "Client UI API"
source_repo: "client"
source_paths:
  - "src/ui/api/"
runtime: "node"
status: "extracted"

description: >
  API endpoints — hello, llm, ssot/save, subagents, test.
  Backend API layer for the client UI.

dependencies:
  databases:
    - schema: "clnt"
      tables: ["client"]
      access: "READ/WRITE"
  apis: []
  secrets:
    - "DOPPLER_TOKEN"

log_targets:
  - "log/014-client-ui-api/"
