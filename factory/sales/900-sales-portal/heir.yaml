# HEIR — 900 Sales Portal
# Aviation Model VIN — 8 fields, stamped at build, never changes.
# Extended fields from prior heir.yaml preserved in _archived-fragments/CLAUDE.md.
sovereign_ref: "imo-creator-v2"
hub_id: "sales-portal-900"
ctb_placement: "leaf"
imo_topology: "egress"
cc_layer: "CC-04"
services:
  - "CF Worker (HTTP GET/POST)"
  - "D1 sales-portal-900 (working tables)"
  - "Neon PostgreSQL (outreach vault, read-only)"
secrets_provider: "doppler"
acceptance_criteria:
  - "Meeting 1 pre-populates form from outreach 5 sub-hubs via sovereign_id"
  - "Meeting 1 form writes validated data to sales_factfinder in D1"
  - "Meetings 2-4 are read-only presentation pages — no write path"
  - "Meeting 2 renders Monte Carlo results when simulation engine is built"
  - "Meeting 4 shows quoted costs by insurance line grouped by benefit_type"
  - "Phase sequence enforced: factfinder -> insurance -> systems -> financials -> closed (no skipping)"
  - "All meeting pages branded per prospect slug"
  - "sales_state_errors CQRS gap resolved before OPERATE transition"
  - "Worker deployed and /health returns 200 before OPERATE transition"
