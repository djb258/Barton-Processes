# ORBT — Process 840 Client Record Key
# OBD diagnostic state: Operate | Repair | Build | Troubleshoot_Train

process_id: "CLIENT-RECORD-KEY"
process_name: "Client Record Key — Canonical Client Record Schema"
silo: "svg-agency"

state: "BUILD"
strikes: 0
last_transition_at: "2026-05-12T00:00:00Z"
last_transition_reason: "BAR-bp840 — vaulted_at added (48 fields), doc verified against live svg-d1-client schema, UT v2.1.0, ready for Codex audit"

# ORBT state definitions:
# BUILD           — scaffolded, not yet deployed or not all gates clear
# OPERATE         — running clean, all gates green, metrics within tolerance
# REPAIR          — active fix in flight; known break being resolved
# TROUBLESHOOT_TRAIN — Strike 3 reached; fleet-wide fix required; Airworthiness Directive pending

# Current blockers (must clear before BUILD → OPERATE):
blockers: []

# Note: Content already exists and is complete (migrated from v1.0.2).
# BUILD → OPERATE gate requires:
# (1) All 4 schemas verified live against actual DB
# (2) Canonical field table confirmed against live migrations
# (3) Auditor sign-off (different engine than builder)
