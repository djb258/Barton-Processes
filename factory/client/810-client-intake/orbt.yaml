# ORBT — Process 810 Client Data Intake
# OBD diagnostic state: Operate | Repair | Build | Troubleshoot_Train

process_id: "PROC-810"
process_name: "Client Data Intake"
silo: "svg-agency"

state: "BUILD"
strikes: 0
last_transition_at: "2026-04-29T00:00:00Z"
last_transition_reason: "UT consolidation — PROCESS-UT.md and DOCTRINE.md written; fragments archived"

# ORBT state definitions:
# BUILD           — scaffolded, not yet deployed or not all gates clear
# OPERATE         — running clean, all gates green, metrics within tolerance
# REPAIR          — active fix in flight; known break being resolved
# TROUBLESHOOT_TRAIN — Strike 3 reached; fleet-wide fix required; Airworthiness Directive pending

# Current blockers (must clear before BUILD → OPERATE):
blockers:
  - id: "FP-810-01"
    description: "D1 database_id empty in wrangler.toml — run `wrangler d1 create client-intake-810`"
    severity: "DEPLOY_BLOCKER"
  - id: "FP-810-02"
    description: "No authentication on any endpoint — Bearer token or CF Access required"
    severity: "SECURITY_BLOCKER"

# ORBT gate: BUILD → OPERATE requires all blockers cleared + 3 runs within tolerance + auditor sign-off
