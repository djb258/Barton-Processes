name = "client-portal-830"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[[d1_databases]]
binding = "D1"
database_name = "client-intake-810"
database_id = ""  # Same D1 as 810 — shared read access

[vars]
# No secrets needed — reads from shared D1
